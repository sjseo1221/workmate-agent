[project]
name = "workmate-agent"
version = "0.1.0"
description = "Workmate AI A2A agent"
requires-python = ">=3.12,<3.13"
dependencies = [
    "a2a-sdk[http-server]==1.1.2",
    "fastapi==0.116.1",
    "psycopg[binary]==3.2.10",
    "uvicorn==0.35.0",
]

[dependency-groups]
dev = [
    "jsonschema==4.26.0",
]

[tool.uv]
package = false
